apiVersion: v2
name: moirai
description: A Helm chart for the Morai - GenAI Press Review service
type: application
version: 0.1.133
appVersion: "1.0.0"

# Define the dependencies for this chart
dependencies:
  - name: couchdb
    version: "{{ .Values.couchdb.chartVersion }}"
    repository: "https://apache.github.io/couchdb-helm"
