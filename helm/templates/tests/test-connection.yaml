# templates/tests/test-connection.yaml
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moirai.fullname" . }}-test-connection"
  labels:
    {{- include "moirai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
    "helm.sh/hook-weight": "0"
spec:
  initContainers:
    - name: wait-for-service
      image: busybox
      command: ['sh', '-c', 'until nc -z -v -w30 {{ include "moirai.fullname" . }} {{ .Values.service.port }}; do echo "Waiting for service..."; sleep 5; done']
      resources:
        requests:
          memory: 64Mi
        limits:
          memory: 128Mi
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moirai.fullname" . }}:{{ .Values.service.port }}']
      resources:
        requests:
          memory: 64Mi
        limits:
          memory: 128Mi
  restartPolicy: Never
