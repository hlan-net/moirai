# In secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-couchdb-secret
  labels:
    app: {{ .Release.Name }}-{{ .Values.couchdb.name }}
    {{- include "common.labels" . | nindent 4 }}
    app.kubernetes.io/component: couchdb
type: Opaque
data:
  username: {{ .Values.couchdb.adminUser | b64enc }}
  password: {{ .Values.couchdb.adminPassword | b64enc }}